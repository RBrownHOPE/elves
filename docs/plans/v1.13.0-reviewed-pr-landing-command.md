# v1.13.0 Reviewed PR Landing Command

Branch: `codex/v1.13.0-land-reviewed-pr-command`

## Objective

Make the common "review the diff from main, read PR comments, fix, test, and merge commit once
green" instruction an explicit Elves workflow.

## Scope

- Add the Reviewed PR Landing Command to `SKILL.md` and the Codex companion `AGENTS.md`.
- Clarify that the command is a one-off merge opt-in for the current PR, while the default remains
  human-controlled merging.
- Sync README, review protocol, kickoff template, survival-guide template, and plan template.
- Bump the release metadata to `1.13.0`.
- Extend the repo consistency checker so future docs cannot drift on the new command.

## Verification

- `python3 -m py_compile scripts/check_repo_consistency.py scripts/install_doctor.py scripts/sync_installed_skills.py`
- `python3 scripts/check_repo_consistency.py`
- `python3 -m unittest discover -s tests -p 'test_*.py'`

## Out Of Scope

- No new service, CLI, or GitHub automation code.
- No change to the default Elves rule that the user merges unless there is explicit opt-in.
