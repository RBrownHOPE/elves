# READ THIS FILE FIRST AFTER ANY COMPACTION OR RESTART

This Survival Guide is the authoritative live brief for the v1.20.1 Cobbler runtime hardening run.
If chat memory conflicts with this file, trust this file and canonical disk state.

Read order after restart: this file -> `.elves-session.json` -> `docs/elves/learnings.md` ->
`docs/plans/v1.20.1-cobbler-runtime-hardening.md` -> execution log -> `.ai-docs/manifest.md` and linked
docs if present -> `TODO.md`.

## Mission

Repair the merged v1.20.0 external-agent runtime so every council/model/session/write/release claim is
backed by real evidence, while preserving Elves' native-only, zero-config default. Ship a natural,
optional capability-routing experience for both Codex and Claude Code; prove the available
Claude/Fable, Grok 4.5, and Sakana/Fugu routes live; harden the persistent-session and writer
transactions; update all docs; and release v1.20.1 locally and on GitHub.

The host coordinator owns run memory, route decisions, acceptance, branch refs/commits, pushes, PR,
merge, tag, release, and installed-skill synchronization. External workers are optional untrusted
patch producers and never own those surfaces.

## Run Control

- **Run mode:** finite
- **Stop policy:** plan-complete-or-true-blocker
- **User intent:** “Do a follow on PR and land using our Elves ceremony, and bump version number
  locally and on GitHub. Make sure README and other docs are up to date before merging, and make
  sure this works for Codex and Claude Code. It should be easy and natural to use, using whatever
  tools the user has available, helpful without being intrusive, and optional.”
- **Checkpoint due by:** approximately 8 hours after the fresh launch call; no fixed clock time was
  supplied
- **Checkpoint semantics:** delivery target only, not an artificial stop boundary
- **May continue after checkpoint:** yes, when safe progress toward this finite plan remains
- **Actual stop conditions during execution:** all five product batches, final readiness, regular
  merge, v1.20.1 tag/GitHub release, local Codex+Claude sync, and post-release verification complete;
  explicit user stop; or a genuine blocker with no viable in-scope recovery
- **Workspace ownership:** branch `codex/cobbler-runtime-hardening-v1.20.1` in dedicated worktree
  `/Users/john/aigora/dev/elves-v1.20.1-hardening`; no other writer may use this checkout/branch
- **Branch tip at start (collision tripwire):**
  `fca15a9e6175c528079bc7ca87e0925f83b9f9b9`
- **Merge policy:** merge-commit-on-green; the user explicitly authorized this run to land. Use a
  regular merge commit only after final readiness; never squash or rebase.
- **Release policy:** v1.20.1 local annotated tag and GitHub release are explicitly authorized only
  after the verified merge commit exists on main. Do not publish/tag the branch tip.
- **Installed-skill policy:** leave current v1.19.0 Codex/Claude installs untouched until the branch
  is fully green. Then sync/test v1.20.1 before merge and re-verify from released main afterward.
- **Final-response policy:** staging-only response is allowed now because Elves requires a fresh
  launch call. After launch, final response is disallowed until all actual stop conditions are met.
- **Coordination mode:** Cobbler-first; use bounded independent lenses for non-trivial contracts,
  risks, debugging, review, and synthesis; main coordinator owns the fitted decision.
- **Bootstrap rule:** v1.20.0's writer boundary is untrusted. Implement Batches 1–3 with native Codex
  workers/direct host work. Route a substantial batch to Grok only after Batch 3's corrected writer
  boundary and exact session pass qualification.
- **Progress visibility:** promptly push meaningful
  `[codex/cobbler-runtime-hardening-v1.20.1 · Batch N/5 · phase] concrete outcome` commits. Use
  `Contract`, `Implement`, `Validate`, `Review`, and `Close`; avoid vague WIP/noisy micro-commits.
- **Batch completion rule:** update execution log -> update this guide/session JSON -> `Close`
  commit -> push -> re-read this file. Completed work cannot remain only in the working tree.
- **Re-read rule:** immediately after every commit and push, re-read this file before the next action.
- **Checkpoint rule:** the estimated checkpoint is delivery-only; log and push useful evidence, then
  continue immediately while work remains. It becomes a stop only if this Run Control block is
  explicitly rewritten to make it a hard boundary.
- **Continuation rule:** after launch, if work remains and no actual stop condition applies, continue
  without waiting for user acknowledgment.

## Cobbler Session State

- **Cobbler default:** on
- **Activated by:** explicit Elves invocation and request to use the Elves ceremony
- **Scope:** current Elves run
- **Behavior:** treat run follow-ups as Cobbler-mediated; answer simple matters directly, use
  parallel independent lenses when valuable, and delegate only bounded owned work
- **Persistence:** this Survival Guide and `.elves-session.json`
- **Exit phrases:** “Cobbler Mode: off”, “leave Cobbler Mode”, “stop using Cobbler by default”

## Session Budget

- **Staging started:** 2026-07-12 12:30 EDT
- **Execution starts:** fresh user launch call after staging handoff
- **User returns:** approximately 8 hours after launch unless superseded
- **Checkpoint expectation:** a merged and released v1.20.1 if every required live/provider/host gate
  passes; otherwise a clean, pushed, evidence-backed blocker state
- **Time budget:** approximately 8 hours, with judgment to finish a near-complete finite run safely
- **Average batch time so far:** N/A; execution not launched
- **Batches remaining:** 5 of 5

## Stop Gate

- **Planned batches remaining:** 5
- **Stop allowed right now:** yes during staging only
- **Why:** Elves requires staging and execution to be separate calls; the branch/PR/run documents and
  short launch prompt are the staging deliverable
- **Next required action:** user sends the fresh launch prompt; the launch call immediately changes
  this gate to `no` and begins Batch 1

## Effort Standard

- Work as hard as you can for the full run. Do not be lazy; keep implementation, validation, and
  review rigorous.
- Do not accept the first green suite while direct runtime behavior or live evidence remains false.
- Do not settle for the minimum acceptable change, the first green result, or a shallow pass when
  deeper verification or the next planned task remains.
- Maintain the same effort in Batch 5 and post-merge verification as in Batch 1.
- When one task finishes, take the next highest-value action from the plan or review queue immediately.

## Forbidden Stop Reasons

After launch, none of these permits stopping while work remains:

- A checkpoint, commit, push, PR, green CI run, useful summary, or Elves Report exists.
- One batch is complete and a later batch remains.
- An optional public route falls back successfully.
- The user is silent, the work feels large, or a batch boundary feels natural.
- A fake/fixture suite is green while required live/provider/host evidence remains absent.

Use the Stop Gate and actual stop conditions, not intuition.

## Coordinator-to-Implementer Handoff Standard

The host coordinator writes every worker packet for an implementation agent that may be less capable,
hold no chat context, and misread a green test as product correctness. Every packet includes:

1. product intent and why the batch matters;
2. current branch/HEAD, plan/run-document paths, and relevant source files;
3. existing abstractions/utilities to build on and patterns not to duplicate;
4. exact owned files/responsibilities and forbidden files/surfaces;
5. observable behaviors and acceptance evidence, not merely “make tests pass”;
6. known v1.20.0 failure modes and the shortcuts that would recreate them;
7. expected validation/review commands and likely tool/version pitfalls;
8. intended meaningful commit boundaries and outcome-focused subjects;
9. route/model/session/worktree/capability evidence when an external worker is involved;
10. the rule that uncertainty is reported, not papered over with fabricated success.

The packet may be detailed without prescribing brittle line-by-line code. Workers survey current
source and use judgment inside the contract. Canonical plan, Survival Guide, execution log,
learnings, and `.elves-session.json` remain host-only.

## Memory Surfaces

- **Plan:** `docs/plans/v1.20.1-cobbler-runtime-hardening.md`
- **Survival Guide:** this file
- **Learnings:** `docs/elves/learnings.md`
- **Execution log:** `docs/elves/v1.20.1-cobbler-runtime-hardening-execution-log.md`
- **Structured state:** `.elves-session.json`
- **Ignored evidence/runtime:** `.elves/runtime/` and `docs/audit/`
- **Durable docs:** `.ai-docs/*` when present

Promotion flow: execution log -> learnings -> curated `.ai-docs/*`.

## Strategic Forgetting

- Rewrite live sections in place; chronology belongs in the execution log.
- Promote only stable reusable facts to learnings or `.ai-docs`.
- Reconcile idle child processes, live leases, paid jobs, and worktrees after every external turn.
- Keep persistent on-disk sessions distinct from active compute.
- Archive/condense oversized execution evidence and leave a reactivation handoff when a fresh thread
  would be faster.
- Never delete unrelated app/session/worktree/skill/plugin state. Installed-skill changes in this run
  are limited to the explicitly authorized managed Codex and Claude Elves mirrors after green.

## Non-Negotiables

- No fabricated evidence. A model, session, council vote, smoke, write capability, acceptance box,
  release claim, or local install is successful only when the behavior actually occurred.
- Native-only use stays complete, quiet, and zero-config. Public external routes are optional; this
  run's final live matrix is required only because the user explicitly requested qualification.
- No secret values in config, argv, prompts, artifacts, logs, JSON, git, PRs, or release notes.
- External workers never own run memory, branch refs, pushes, PR, merge, tag, or release. Only the
  repaired host-controlled audit/patch-import boundary may integrate their output.
- Preserve user comments/config/aliases. Ambiguity fails closed with a redacted proposal.
- Codex and Claude Code behavior/docs/install surfaces must agree while retaining host-honest syntax.
- Merge with a regular merge commit only after final readiness; tag/release only that merge commit.

## Launch Readiness

- [x] Detailed v1.20.1 plan written from merged-state audit and three independent planning lenses
- [x] Dedicated branch/worktree created from current `origin/main`; collision tripwire recorded
- [x] Survival Guide, execution log, learnings path, and structured session path defined
- [x] Staging baseline/preflight recorded against the dedicated worktree: 278/278 tests and all
      deterministic checks green; only expected install-deferred/no-project-marker advisories
- [x] Planning documents and session state committed/pushed with outcome-focused Batch 0 commit
      `72553fc836c5beadf9d0ddaf6d57061ea8069d92`
- [x] Draft follow-on PR #60 opened and recorded
- [x] PR branch/tip ownership confirmed; PR is mergeable, Socket checks green, CodeQL in progress,
      and no check failed at the staging observation
- [x] No active paid/long-running process remains ambiguous
- [x] Short fresh-call launch prompt updated with PR #60 in this staging-close commit
- [x] Fresh-call requirement recorded: staging may stop for handoff; launch begins with the Stop Gate initialized with `Stop allowed right now: no` and `continuation_guard.stop_allowed: false`

## Current Phase

**Status:** Staging complete after this close commit is pushed; no product implementation has started.

**Active batch:** Batch 0 — plan, run memory, preflight, branch, and PR.

**What was just finished:** merged-state audit, v1.20.1 scope, dedicated worktree, three planning
lenses, green preflight/baseline, first staging commit/push, and draft PR #60.

**Single next action:** push this staging-close commit, verify local/remote tip and PR checks, then
hand the user the fresh launch prompt; do not implement Batch 1 in this staging call.

## Active Compute

No paid inference, model CLI, live external-writer lease, dev server, or long-running child process is
active at staging. Planning subagents were read-only and are complete/closed before handoff.

## Next Exact Batch

**Batch:** Batch 1 — Effective configuration and truthful read-only dispatch.

**Scope:** Normalize effective routes and attempt chains, replace invalid adapter argv, remove canned
host evidence, execute fallbacks, prove actual models, scope named credentials, and harden process/
artifact lifecycle without changing the complete native-only host experience.

**Acceptance criteria:** Every Batch 1 acceptance row in the plan has concrete focused/full test and
review evidence in `.elves-session.json`; no unsupported CLI flag, fake host quorum, dropped route
field, secret value, surviving descendant, or run-ID collision remains.

**Risk:** CLI output/model metadata is version-sensitive. Unknown evidence must fail exact-model
qualification and fall back, never be inferred from requested configuration or model-authored text.

**Contract first:** Before source edits, write the Batch 1 contract in the execution log from the
plan's intent, Build On list, tasks, forbidden shortcuts, acceptance criteria, blast radius, and
expected progress commits. Reproduce the invalid argv/canned-host/field-dropping behavior with focused
tests, then repair root causes.

## Model Routing for This Run

This is a project/run override, not the shipped public default:

```yaml
model-routing:
  enabled: true
  public-default: host-native-zero-config
  document-owner: codex-host-coordinator
  supervise:
    route: current-codex-host
    required: true
  implement:
    batches-1-through-3: native-codex-workers-or-host
    reason: v1.20.0 external writer boundary is under repair and cannot bootstrap itself
    after-batch-3:
      preferred: qualified-persistent-grok-4.5
      unit: one-substantial-batch-or-canary
      required-for-release-smoke: true
      fallback-for-product-edits: native-codex-worker-or-host
  validate:
    route: deterministic-host-shell
    required: true
  review:
    preferred-parallel-routes:
      - fresh-native-host-reviewer
      - claude-code:fable-or-opus
      - sakana:fugu-ultra
    exclude: exact-grok-implementation-worker
    required-quorum: 2 for high-risk batch close and final cumulative review
  live-release-matrix:
    routes: [claude-fable-or-opus, grok-4.5, fugu-ultra]
    required: true for this run
    public-default-required: false
  git-pr-merge-tag-release:
    route: deterministic-host-shell
    required: true
```

If a provider route changes, record requested route, actual route, and the material reason in the
execution log/session JSON. Do not let native fallback conceal failure of this run's required final
live matrix.

## Validation Gates

Run focused gates per batch and the full gates before every close:

```bash
python3 scripts/check_repo_consistency.py
python3 -m unittest discover -s tests
python3 -m compileall -q scripts tests
bash -n scripts/preflight.sh scripts/notify.sh
python3 -m json.tool config.json.example
git diff --check
```

At release add v1.20.1 release checklist, TOML/config resolution, credential scan, temporary and real
Codex/Claude install sync, Elves landing check, live provider/host matrix, full PR surfaces/checks,
cumulative independent review, clean resource/worktree checks, and post-merge tag/release identity.

## Post-Checkpoint Control Loop

Every completed batch must end with a commit and push. After that push, re-read this survival guide before doing anything else.

After every meaningful progress commit and every completed batch:

1. push the host-owned commit promptly;
2. re-read this Survival Guide before any other action;
3. verify local and remote branch tips equal the commit just created and no unknown writer moved the
   collision tripwire;
4. poll all PR comments, threads, reviews, checks, and bot surfaces that may have updated;
5. reconcile external processes, persistent sessions, live leases, paid jobs, and worktrees;
6. update the execution log, this guide, and `.elves-session.json` with route/fallback/usage and
   acceptance evidence;
7. identify the first incomplete batch and write its detailed contract before source edits;
8. ask: does the Stop Gate still say `Stop allowed right now: no`? If yes, continue immediately
   unless an actual stop condition applies.

The estimated eight-hour checkpoint is a delivery target, not completion. A clean commit, green CI,
or useful summary does not change the gate.

## After Any Compaction

1. Read this file first, including the Run Control section and Stop Gate, then
   `.elves-session.json` (especially `continuation_guard`), learnings, plan, and execution log in the
   order stated at the top.
2. Confirm the owned worktree/branch and compare `HEAD` and remote tip with the recorded expected tip.
3. Re-run `git status --short --branch`; do not overwrite unexpected user/agent changes.
4. Inspect active compute, session, and lease state before launching or resuming any external tool.
5. Read the current batch contract and its last acceptance/review evidence; do not repeat completed
   work or trust unstored chat memory.
6. Resume the single next action from `Current Phase`/`Next Exact Batch`. While work remains and the
   continuation guard is false, do not end the turn for a checkpoint or summary.

## Final Landing and Release Rules

1. Do not bump version until Batches 1–4 and live prerequisites are truthful and green.
2. Before merge, update every README/skill/agent/config/provider/session/writer/install/release surface.
3. Poll overview comments, inline comments/threads, issue comments, bot comments, reviews, and checks
   after every relevant push; fix real blockers and re-read all surfaces.
4. Run final cumulative review against `main...HEAD`, Elves landing check, and Elves Report.
5. Remove this operational Survival Guide, execution log, and `.elves-session.json` from the final
   diff after their evidence is consumed; retain the plan and durable learnings.
6. Merge only when non-draft, clean, checks green, no requested changes/blockers, and final review
   clean. Use `gh pr merge --merge`.
7. Verify the merge SHA on main, create/push annotated `v1.20.1`, publish the GitHub release, sync both
   installed host copies from released main, rerun bounded host smoke, and reconcile resources.

## Fresh-Call Launch Prompt

> `$elves` Start PR #60, the staged v1.20.1 Cobbler runtime hardening run, on
> `codex/cobbler-runtime-hardening-v1.20.1`. Read
> `docs/elves/v1.20.1-cobbler-runtime-hardening-survival-guide.md`, `.elves-session.json`,
> `docs/elves/learnings.md`, `docs/plans/v1.20.1-cobbler-runtime-hardening.md`, and
> `docs/elves/v1.20.1-cobbler-runtime-hardening-execution-log.md` in that order. Immediately change
> the Stop Gate and continuation guard to disallow stopping while work remains. Execute all five
> batches Cobbler-first with detailed contracts for potentially less-capable workers, meaningful
> pushed progress commits, focused+full validation, parallel independent review, and truthful live
> evidence. Bootstrap Batches 1–3 with native Codex workers because the v1.20.0 writer is under
> repair; use persistent Grok only after the repaired boundary qualifies. Keep every public external
> route optional and native-only complete. The user explicitly authorizes merge-commit-on-green plus
> local/GitHub v1.20.1 tag/release, but merge only after final readiness and release only the verified
> merge commit. Do not stop for checkpoints, clean commits, green CI, summaries, or silence; stop only
> after merge/release/local-host verification is complete, explicit user stop, or a genuine blocker.
