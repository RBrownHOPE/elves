# Execution Log: v1.20.1 Cobbler Runtime Hardening

Chronological evidence for the finite v1.20.1 follow-on run. The plan owns scope; the Survival Guide
owns live control; `.elves-session.json` owns machine-readable batch/acceptance state; this file owns
what happened and why.

## Run Identity

- **Session:** `elves-2026-07-12-cobbler-runtime-hardening-v1-20-1`
- **Branch:** `codex/cobbler-runtime-hardening-v1.20.1`
- **Worktree:** `.` relative to this execution log's repository root; canonicalize and verify branch
- **Base/collision tripwire:** `fca15a9e6175c528079bc7ca87e0925f83b9f9b9`
- **Plan:** `docs/plans/v1.20.1-cobbler-runtime-hardening.md`
- **Survival Guide:** `docs/elves/v1.20.1-cobbler-runtime-hardening-survival-guide.md`
- **Learnings:** `docs/elves/learnings.md`
- **PR:** #60 — https://github.com/aigorahub/elves/pull/60 (draft at staging)
- **Target release:** v1.20.1
- **Merge authorization:** explicit merge-commit-on-green
- **Release authorization:** local annotated tag plus GitHub release after verified merge commit

## Staging — 2026-07-12

### Inspect merged/release state

- Fetched `origin/main` and tags.
- Confirmed clean `main` equals `origin/main` at
  `fca15a9e6175c528079bc7ca87e0925f83b9f9b9`.
- Confirmed local/GitHub `v1.20.0` tag/release exists and `v1.20.1` is free.
- Confirmed no conflicting checkout of the new branch. Existing old v1.20.0 worktrees are separate.
- Elves install doctor reported the expected advisory: installed Codex skill v1.19.0 while v1.20.0
  is published. No install was updated because v1.20.1 must first repair v1.20.0.
- Created dedicated worktree/branch with the helper after dry-run:

  ```text
  branch: codex/cobbler-runtime-hardening-v1.20.1
  worktree path: /Users/john/aigora/dev/elves-v1.20.1-hardening
  base ref: origin/main
  collision tripwire: fca15a9e6175c528079bc7ca87e0925f83b9f9b9
  ```

### Reconfirm baseline and live CLI contracts

- `python3 -m unittest discover -s tests`: 278 passed, 0 failed, 0 skipped in 12.105s.
- `python3 scripts/check_repo_consistency.py`: OK, version 1.20.0.
- `python3 -m compileall -q scripts tests`: pass.
- `python3 scripts/release_checklist.py`: v1.20.0 checklist pass.
- Installed CLI versions:
  - Claude Code 2.1.207
  - Grok 0.2.93
  - Codex CLI 0.144.1
- Read-only `--help` probes proved the current native command surfaces and directly contradicted the
  v1.20.0 invented adapter flags. No paid/model turn was launched during staging.

### Merged-state audit incorporated

The follow-on plan treats these previously reproduced defects as blockers, not suggestions:

- invalid Claude/Grok/Codex read-only and session argv;
- canned host PASS reports satisfying council quorum;
- dropped route profile/model/executable/required/fallback fields and unexecuted fallback chains;
- missing authoritative actual-model evidence;
- incomplete environment credential delegation and descendant cleanup/run-ID collision;
- session rehydration bypass, non-atomic registries, and invalid exact-resume construction;
- `.elves` writer path escape, arbitrary session/write qualification, lease races;
- missed git common-dir mutations, export/audit state bypass, stale patches, and non-cumulative
  dependent patch apply-check;
- destructive/unsafe setup serialization, ignored config semantics, fake smoke/inventory claims,
  secret-shaped output risk, and missing project Claude setup aliases;
- previous acceptance boxes marked complete without the required live/mirror evidence.

### Parallel planning lenses

Three independent read-only lenses inspected distinct source areas; no lens edited files.

1. **Dispatch/adapter lens — Chandrasekhar**
   - Replace each built-in with explicit argv/stdin/decoder contracts and fail unknown versions into
     fallback rather than guessing.
   - Preserve the full resolved route through ordered attempts.
   - Delete canned host-native evidence; standalone native mode may complete directly but cannot be
     called council-verified without a real injected host execution identity.
   - Derive actual model from adapter machine metadata, not model-authored JSON.
   - Run fallbacks sequentially within a logical lane while independent lanes remain parallel.
   - Use process groups, UUID-backed artifact roots, and per-profile named-secret allowlisting.
   - Strongest dissent: a CLI without trustworthy model metadata may still offer unpinned optional
     advice but cannot satisfy an exact-model gate.

2. **Session/writer lens — Beauvoir**
   - Use one common-repository durable store with atomic restricted writes, registry locking,
     collision-safe IDs, revisions/CAS transitions, and malformed-record fail-closed behavior.
   - Keep rehydration required sticky with pending digest/HEAD plus nonce; only matching actual
     acknowledgment adopts pending state.
   - Derive exact resume from the registry and versioned adapter contract; CWD is subprocess metadata,
     not unsupported argv.
   - Bind lease identity immutably and make one lease win across all linked worktrees.
   - Use an explicit linear audit/export/apply/integrate state machine; capture common-dir plus
     worktree git state; hash an immutable patch manifest and cumulatively apply-check it while
     leaving host state byte-identical.

3. **Setup/host/release lens — Maxwell**
   - Keep v1.20.1 a backward-compatible truthfulness/safety patch, not a provider platform.
   - Separate detected, probed, qualified, and smoked states; unknown is not qualification.
   - Keep core routing provider-neutral through a small JSON/stdio-style wrapper contract; built-ins
     are compatibility presets.
   - Make setup inspect-first with explicit apply/probe/real-smoke operations and atomic digest-
     checked persistence that preserves user bytes or refuses safely.
   - Normalize enabled/session/context/config-sharing semantics and read v1 without silent rewrite.
   - Align global/project Codex/Claude installs, all seven Claude aliases, temporary install tests,
     README/CHANGELOG/references, and post-merge tag/release/local sync.

### Planning synthesis

Accepted all three common recommendations: truth over schema-shaped placeholders, full provenance,
native-only completion distinct from council verification, atomic/CAS state transitions, safe
inspect-first setup, provider-neutral customization, host parity, and post-merge release identity.

The final plan uses five substantial architecture-ordered batches. The staging plan proposed a native
bootstrap because the external writer component is under repair. The fresh launch instruction
supersedes that implementation route: use one persistent Grok worker for every batch while the host
provides an independent manual outer safety boundary until the shipped runtime qualifies itself.

## Launch — 2026-07-12 13:00 EDT

- Fresh launch accepted for PR #60. Run status changed from `staging_ready` to `in_progress`; Stop
  Gate and continuation guard changed to `no`/`false` before product implementation.
- Latest user objective is to test the real procedure, not merely obtain equivalent code: the same
  persistent Grok Build agent receives one detailed batch at a time, edits/tests/commits in an
  isolated worker repository, and resumes with accumulated context across the run.
- Host responsibilities remain unchanged: canonical run documents, contracts, worker packet,
  mechanical safety audit, patch import, deterministic gates, independent review council, PR
  comments/checks, branch commits/pushes, merge, tag, release, and installed-skill verification.
- Bootstrap safety decision: do not invoke the known-broken v1.20.0 writer CLI as if it were trusted.
  Instead create an independent local clone with no remote, run Grok there with GitHub credential
  variables absent, require detached commits, audit the exact base-to-tip chain and repo state, then
  import reviewed binary patches into the host branch. This exercises the intended procedure while
  preventing the component under repair from certifying itself.
- Verify Green at launch: 278 passed, 0 failed, 0 skipped in 10.548s; consistency, compileall, shell,
  JSON, and whitespace pass. PR #60 is mergeable; CodeQL and Socket checks are all green.
- PR feedback cycle 0 found one real portability issue: review comment `3566717640` flagged the
  absolute `owned_worktree`. The contract commit changes it to repository-relative `.` with explicit
  canonicalization/branch verification. Reply/resolve after push.
- Rollback tag created: `elves/v1.20.1-pre-batch-1` at
  `2aca0bf907633fdc6dfe1bc1ab46b44a035699b0`.
- Active plan hash after recording the launch routing override:
  `02ac6a226ba8265218e5abc28b4a56331efcc8f899a1a1e66808ef1755675b67`.

### Persistent Grok qualification

- Created independent clone `../elves-v1.20.1-grok-worker`, detached it at launch-contract commit
  `d8024b58c34a948e0e214d74f35a6fe3b77e458f`, removed its only remote, installed a local
  non-credentialed worker identity, reset the local credential helper, and supplied a private empty
  `GH_CONFIG_DIR` plus a minimal environment. The ignored prompt/evidence root is private to the clone.
- Baseline evidence: clean detached status; no remotes; refs SHA-256
  `11b00b197706d07b228ff92c07ab88ea381de2dd52185c032da96201a487abc8`; local-config SHA-256
  `19cbc9356680206d7a310907260459480383d3004228e34ad0671e7ec981c94b`; hooks SHA-256
  `869f208cd3158287f078a6adfae73e6b08f5abafa363a85932457e5a5a09af18`.
- `grok models` proved the subscription login is active and exposes `grok-4.5` as the default plus
  `grok-composer-2.5-fast`; Grok Build is `0.2.93 (f00f96316d4b) [stable]`.
- Created persistent session `00b682ea-b3df-4c3f-8009-7a0f51dd8f63` with requested model
  `grok-4.5`, high reasoning, `devbox` sandbox, no subagents, no cross-session memory, and no web
  search. The first headless turn used `dontAsk` and was cancelled before its read-only tool request.
  This is a real procedure finding: `dontAsk` cannot be the autonomous headless default for this CLI.
- Audited the cancelled turn: clean detached HEAD, no remote, and all three baseline hashes unchanged.
  Resumed the exact same UUID with `auto` permission inside the unchanged outer boundary; the worker
  read the plan/contract/runtime and returned a detailed ready report without editing or committing.
- Actual-model evidence is Grok-owned metadata, not worker prose: session `summary.json` records
  `current_model_id=grok-4.5`, `reasoning_effort=high`, `sandbox_profile=devbox`, expected git root and
  HEAD; assistant events record `model_id=grok-4.5` and fingerprint `fp_a39489019fa99b6e`.
- Final qualification audit again found clean detached HEAD, no remotes, unchanged refs/config/hooks,
  and no active process for this session. Existing unrelated Grok TUI processes were left untouched.
- PR cycle after launch push: replied to portability comment `3566717640` with fix commit `d8024b5`
  in reply `3566734484`, then resolved review thread `PRRT_kwDORs6Fzc6QN51c`. New CodeQL runs were
  pending after the push; both Socket checks were green and no check had failed.

## Batch Registry

### Batch 1: Effective configuration and truthful read-only dispatch

- **Status:** in progress — contract and persistent Grok qualification
- **Started:** 2026-07-12T17:00:12Z
- **Tag at start:** `elves/v1.20.1-pre-batch-1` at `2aca0bf907633fdc6dfe1bc1ab46b44a035699b0`
- **Primary surfaces:** schema/config/capabilities/context/adapters/dispatch/operator CLI/tests
- **Expected progress slices:** Contract; normalized config+attempt graph; real adapter contracts;
  host/model/fallback truthfulness; process/env hardening; Validate; Review; Close
- **Close evidence:** full plan acceptance rows, focused/full counts, adapter help/fixture proof,
  process/secret tests, two independent review reports

**Contract — behaviors:**

- Preserve every effective role/profile field from config through ordered attempt execution and
  result provenance; disabled external routing launches nothing.
- Build version-aware argv/stdin/decoder contracts for Claude Code 2.1.207, Grok Build 0.2.93,
  Codex 0.144.1, and generic wrappers without shell interpolation or invented flags.
- Remove canned host PASS. Native-direct completion remains available at the skill/host layer, but a
  standalone CLI with no injected real host report cannot claim an independent vote or verified quorum.
- Execute each logical lane's fallbacks in declared order while logical lanes remain concurrent;
  required external routes cannot silently downgrade to an implicit native vote.
- Prove requested actual model from adapter/CLI metadata independent of model-authored report JSON.
- Allow only explicitly named profile credential variables to the declaring child and never persist
  their values; default child environment remains minimal.
- Generate collision-resistant private artifact roots and terminate/reap complete subprocess groups
  on timeout or cancellation.
- Preserve complete native-only Codex/Claude behavior with no external process or unsolicited
  `.elves` mutation.

**Build on:**

- `HarnessProfile`, `FallbackEntry`, `RoleRoute`, `ResolvedConfig`, and `ValidationIssue` in
  `schema.py`; extend rather than add a parallel route schema. `schema.py` has 16 direct script/test
  consumers, so additions/defaults must remain backward-compatible.
- Existing precedence/provenance resolution in `config.py` (4 direct consumers); normalize there and
  reuse it from CLI dispatch rather than reconstructing routes in `cmd_council`.
- Existing redaction, packet, private-artifact, and environment helpers in `context.py` (3 direct
  consumers); extend named-secret grants without duplicating a secret scanner.
- Existing adapter registry/parsers in `adapters.py` (8 direct consumers); replace stub/invented
  invocation behavior behind stable entrypoints used later by sessions/writer.
- Existing `LaneSpec`, `LaneResult`, quorum, and concurrent task structure in `dispatch.py` (2 direct
  consumers); add ordered attempts/process-group cleanup without making lanes sequential.
- Existing fake-executable and wall-clock patterns in config/dispatch/native-only tests. Keep the
  real CLI help probes separate from paid/live inference.

**Pre-implementation survey:**

- `cmd_council` currently collapses one resolved review/planning route into repeated role lenses and
  explicitly assigns `requested_model=None`; this is the field-loss point to remove.
- `StubAdapter.run_readonly` fabricates a schema-valid PASS and must not remain a quorum path.
- `build_readonly_invocation` is shared with sessions/tests; keep its public compatibility or migrate
  all consumers together with contract fixtures.
- `scrub_environment` currently rejects secret-looking names even in `extra_allowlist`; change the
  policy to an explicit profile grant whose serialized evidence includes names only.
- `run_council` already uses concurrent asyncio tasks; preserve concurrency while each lane walks its
  own fallback attempts sequentially.
- `new_run_id` uses second-resolution UTC timestamps; switch to collision-resistant identity and
  atomically refuse artifact-root reuse.
- No constitution exists. This batch's legality layer is plan/non-negotiable compliance plus native-
  only regression proof.

**Acceptance criteria:** Use every Batch 1 plan criterion verbatim as the closure checklist. In
addition, the Grok handoff must include at least two meaningful detached commits, a clean worker
status, exact session/model evidence, no remote, and an audit/import manifest. No criterion may be
marked met solely because the 278-test baseline remains green.

**Blast radius:** high. `schema.py` (16 consumers), `adapters.py` (8), `config.py` (4), `context.py`
(3), `dispatch.py` (2), and `cobbler_agents.py` are shared runtime/CLI surfaces used by all later
batches. Changes should be additive behind stable names where possible; every caller and all native-
only/custom/fake paths require focused tests plus a full regression run. Security-sensitive surfaces
are child environment construction, model provenance, process cleanup, and private artifact paths.

**Grok worker packet boundaries:** Grok owns product source/tests for this batch in the isolated
worker clone. Grok must not edit the plan, Survival Guide, execution log, learnings, `.elves-session`,
README/CHANGELOG/release versions, refs/remotes/hooks/config, or anything outside the named runtime/
test surfaces. It may create 2–5 direct-descendant detached commits with outcome-focused subjects;
it may not push, branch, tag, merge, or declare acceptance. The host imports only after audit.

### Batch 2: Exact persistent sessions and subprocess lifecycle

- **Status:** pending Batch 1
- **Tag at start:** `elves/v1.20.1-pre-batch-2`
- **Primary surfaces:** sessions, adapter create/resume, registry store, usage, session CLI/tests/docs
- **Expected progress slices:** Contract; versioned session argv; atomic registry; rehydration proof;
  identity/drift enforcement; Validate; Review; Close
- **Close evidence:** exact create/resume fixtures, rehydration adversarial tests, concurrency/crash
  tests, live bounded session evidence, full suite/review

### Batch 3: Transactional external writer boundary

- **Status:** pending Batch 2
- **Tag at start:** `elves/v1.20.1-pre-batch-3`
- **Primary surfaces:** leases/audit/worker CLI/workspace guard integration/tests/docs
- **Expected progress slices:** Contract; canonical paths+store; exact session/canary binding; common-
  dir snapshots; linear audit/export state; cumulative patch import; Validate; Review; Close
- **Close evidence:** race/path/common-dir/state/tamper/dependent-patch tests plus disposable real Grok
  qualification only after deterministic gates

### Batch 4: Inspect-first setup and natural Codex/Claude experience

- **Status:** pending Batch 3
- **Tag at start:** `elves/v1.20.1-pre-batch-4`
- **Primary surfaces:** setup/config migration/doctor/wrapper docs/aliases/install/sync/skills/tests
- **Preferred implementer:** one qualified persistent Grok worker may receive this substantial batch
  through the repaired lease; otherwise native worker implements, with the live write gate still open
- **Expected progress slices:** Contract; config semantics+migration; safe reconcile+redaction;
  honest probe/smoke; setup UX; host install/docs parity; Validate; Review; Close
- **Close evidence:** preservation/concurrency/secret/no-mutation/real-smoke/generic-wrapper/host-install
  matrix plus full suite and independent review excluding implementer

### Batch 5: Live host parity, docs, v1.20.1 release, and landing

- **Status:** pending Batch 4
- **Tag at start:** `elves/v1.20.1-pre-batch-5`
- **Primary surfaces:** live qualification, all docs/version/release/install/PR/landing surfaces
- **Expected progress slices:** Contract; live provider+session+writer matrix; host parity; docs;
  version bump; release validation; final review/remediation; operational cleanup; merge/release/postcheck
- **Close evidence:** every plan row, exact model/session/live evidence, native-only both hosts, full gates,
  sync/install, PR review/checks, landing check, Elves Report, merge SHA, local/GitHub tag/release, local
  installed v1.20.1 identities

## Decisions Made

- Target v1.20.1 because v1.20.0 is the latest published version and no v1.20.1 tag/release exists.
- Treat user language as explicit merge-commit-on-green and GitHub/local release authorization.
- Stage and launch remain separate calls as required by Elves, despite the end-to-end delivery request.
- Do not update the currently installed v1.19.0 skills during staging; avoid making the audited
  v1.20.0 runtime live by accident.
- Keep external routes optional in the shipped product. Make the three-provider live matrix required
  only for this release run because the user explicitly requested those connectors be rock-solid.
- Latest launch routing supersedes native bootstrap: use the same persistent Grok agent for all
  product batches behind a manual no-remote outer safety boundary until the repaired writer qualifies.
- Retain the detailed plan after closeout; remove operational Survival Guide/log/session JSON from
  the final PR diff after landing evidence and Elves Report are generated.
- Tag/release only the main merge commit, never the feature branch.

## Staging Preflight

- **Status:** pass with two expected advisories at 2026-07-12T16:48:43Z
- **Baseline:** 278/278 tests after document creation; 0 failed, 0 skipped
- **Deterministic gates:** consistency, compileall, shell syntax, session/example JSON, current v1.20.0
  release checklist, Survival Guide validator, whitespace, and credential signature scan pass
- **Expected advisories:** installed-skill sync deliberately deferred until green; no package-manager
  marker because this is a script/docs skill with GitHub Actions
- **Plan hash:** `ab91d5ad4e29951b51e2153fc69b6f86b9d611e7b37e91cb72c9145bcd545b85`
- **Git/auth/workspace:** remote/auth/push dry-run, branch staleness, one-worktree ownership, ephemeral
  ignores, and non-interactive environment pass
- **Branch/remote/PR tip:** record after commit/push/PR creation
- **Active compute:** none

### Staging git/PR close

- First operator-visible Batch 0 commit:
  `72553fc836c5beadf9d0ddaf6d57061ea8069d92`
  — `[codex/cobbler-runtime-hardening-v1.20.1 · Batch 0/5 · Contract] Stage v1.20.1 corrective run`.
- Pushed `origin/codex/cobbler-runtime-hardening-v1.20.1`; local and remote tip matched the commit.
- Opened draft PR #60: https://github.com/aigorahub/elves/pull/60.
- GitHub reported the PR mergeable. At the first observation, both Socket checks were green and
  CodeQL actions/python checks were in progress; no check had failed.
- This staging-close update records the PR and fresh launch prompt. Verify local/remote tip and live
  check status after pushing this close commit; product implementation still has not started.

## Reactivation Prompt

> Resume PR #60 from the current Survival Guide. Keep the Stop Gate closed, use the same exact
> persistent Grok Build `grok-4.5` worker for every product batch behind the recorded no-remote
> isolation/audit/import boundary, and keep host ownership of run documents, review, PR/git, landing,
> release, and local host verification. Continue from the first incomplete batch without pausing.

## Completed Archive

- None; execution has not launched.
